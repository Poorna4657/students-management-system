# Example production environment variables
# Replace values with your production credentials (do NOT commit real secrets)

# MongoDB Atlas connection string (use a user with limited privileges)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net
DB_NAME=student_management

# Backend port (internal)
PORT=5000

# NODE_ENV should be production on the droplet
NODE_ENV=production
